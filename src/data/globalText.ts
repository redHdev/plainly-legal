//Default text that will get filled into all agreements
export const globalText = {
    current_date: new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })
};

export default globalText;
